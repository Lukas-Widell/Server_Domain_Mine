# widell.dev static directory

## Scope
This is a very small static page for personal domain presence and basic contact routing.

It does not run a backend, database, API, auth system, analytics pipeline, payments flow, or user data processing.

It does not execute anything remote or dynamic beyond loading local static HTML/CSS/JS files.

It is intentionally boring, limited, and low-risk with no production services behind it.
